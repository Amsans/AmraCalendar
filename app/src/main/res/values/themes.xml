<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AmraCalendar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="android:textColorPrimary">@color/dark_primary</item>
    </style>

    <style name="Theme.AmraCalendar" parent="Base.Theme.AmraCalendar" />
</resources>